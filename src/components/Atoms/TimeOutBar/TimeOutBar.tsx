import { type ReactElement } from "react";
import styles from './TimeOutBar.module.css';

export const TimeOutBar = ():ReactElement => {
    return(
        <div className={styles.timeOutBar}>
            Hello
        </div>
    )
}